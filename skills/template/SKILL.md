---
name: your-skill-name
description: |
  简短描述这个技能的作用以及何时使用它。
  Claude 会在上下文中读取这段描述，以此来决定是否自动触发该技能。
  （注意：尽量将核心用例写在最前面，且尽量控制在 250 个字符以内）
argument-hint: "[可选参数, 例如: issue-number 或 filename]"
disable-model-invocation: false
user-invocable: true
# context: fork  # 取消注释以启用子代理 (Subagent) 隔离运行模式
---

# 🎯 角色与目标

你是一个专注于 [填写特定领域，例如：前端 React 开发 / 数据分析 / 拼写检查] 的专家。
你的主要目标是：[明确描述该技能的最终输出目标]。

# 🔍 触发条件

当用户在发出以下请求时，使用这个 Skill:

- xxx

# 📋 执行步骤

当触发此技能时，请严格按照以下步骤执行：

1. **前置检查**：[例如：检查当前目录下是否存在 package.json]
2. **信息收集**：[例如：使用 grep 搜索相关代码，或阅读同目录下的 style-guide.md]
3. **处理逻辑**：[例如：根据收集到的信息重构代码，确保符合团队规范]
4. **输出结果**：[例如：生成一份 Markdown 格式的审查报告，或直接修改文件]

# 📜 核心原则与规范

- **强制约束 1**：[例如：修改代码前必须先确认文件存在且读取过最新内容]
- **强制约束 2**：[例如：所有输出必须使用中文，代码注释保持英文]
- **禁止事项**：[明确指出不能做什么，例如：不要安装未经允许的第三方 npm 包]

# 📂 参考资源与工具支持

- 如果需要了解详细的架构规范，请读取本目录下的 `references` 和 `templates` 目录下的文件。
- 你可以自动调用 [允许使用的 MCP 工具名称] 来辅助完成任务。
